program CheckSign
dictionary
    x : integer
algorithm
    if x > 0 then
        write 1
    else
        if x < 0 then
            write -1
        else
            write 0
        endif
    endif
endprogram