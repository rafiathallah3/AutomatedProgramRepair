program SumToN
dictionary
    n, i, total : integer
algorithm
    read n
    total <- 0
    i <- 1
    if i <= n then
        total <- total + i
        i <- i + 1
    endif
    write total
endprogram
